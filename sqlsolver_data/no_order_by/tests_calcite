SELECT DEPT.DEPTNO, DEPT.DEPTNO AS DEPTNO0 FROM DEPT AS DEPT
SELECT t0.DEPTNO, t0.DEPTNO AS DEPTNO0 FROM (SELECT * FROM DEPT AS DEPT) t0
SELECT DEPT.DEPTNO, DEPT.DEPTNO AS DEPTNO0 FROM DEPT AS DEPT
SELECT t0.DEPTNO, t0.DEPTNO AS DEPTNO0 FROM (SELECT * FROM DEPT AS DEPT) t0
SELECT DEPT.DEPTNO, DEPT.DEPTNO FROM DEPT AS DEPT
SELECT DEPT0.DEPTNO, DEPT0.DEPTNO FROM DEPT AS DEPT0
SELECT * FROM EMP AS EMP WHERE FALSE
SELECT * FROM (VALUES (NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL)) AS t(EMPNO, ENAME, JOB, MGR, HIREDATE, SAL, COMM, DEPTNO, SLACKER) WHERE 1 = 0
SELECT * FROM (SELECT DEPT.NAME FROM DEPT AS DEPT UNION ALL SELECT DEPT0.NAME FROM DEPT AS DEPT0) AS T1
SELECT * FROM ((SELECT DEPT.NAME FROM DEPT AS DEPT) UNION ALL (SELECT DEPT0.NAME FROM DEPT AS DEPT0)) AS T1
SELECT * FROM EMP AS EMP LEFT JOIN DEPT AS DEPT ON EMP.DEPTNO = DEPT.DEPTNO
SELECT * FROM (SELECT * FROM EMP AS EMP0) AS t2 LEFT JOIN DEPT AS DEPT0 ON t2.DEPTNO = DEPT0.DEPTNO
SELECT EMP.* FROM EMP AS EMP RIGHT JOIN DEPT AS DEPT ON EMP.DEPTNO = DEPT.DEPTNO
SELECT EMP0.* FROM EMP AS EMP0 RIGHT JOIN (SELECT * FROM DEPT AS DEPT0) AS t0 ON EMP0.DEPTNO = t0.DEPTNO
SELECT * FROM EMP AS EMP LEFT JOIN DEPT AS DEPT ON EMP.DEPTNO = DEPT.DEPTNO
SELECT * FROM EMP AS EMP0 LEFT JOIN DEPT AS DEPT0 ON EMP0.DEPTNO = DEPT0.DEPTNO
SELECT EMP.* FROM EMP AS EMP RIGHT JOIN DEPT AS DEPT ON EMP.DEPTNO = DEPT.DEPTNO
SELECT EMP0.* FROM EMP AS EMP0 RIGHT JOIN (SELECT * FROM DEPT AS DEPT0) AS t0 ON EMP0.DEPTNO = t0.DEPTNO
SELECT * FROM EMP AS EMP WHERE FALSE
SELECT * FROM (VALUES (NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL)) AS t(EMPNO, ENAME, JOB, MGR, HIREDATE, SAL, COMM, DEPTNO, SLACKER) WHERE 1 = 0
SELECT * FROM (SELECT DEPT.NAME FROM DEPT AS DEPT UNION ALL SELECT DEPT0.NAME FROM DEPT AS DEPT0) AS T1
SELECT * FROM ((SELECT DEPT.NAME FROM DEPT AS DEPT) UNION ALL (SELECT DEPT0.NAME FROM DEPT AS DEPT0)) AS T1
SELECT * FROM (SELECT DEPT.NAME FROM DEPT AS DEPT UNION ALL SELECT DEPT0.NAME FROM DEPT AS DEPT0) AS T1
SELECT * FROM ((SELECT DEPT.NAME FROM DEPT AS DEPT) UNION ALL (SELECT DEPT0.NAME FROM DEPT AS DEPT0)) AS T1